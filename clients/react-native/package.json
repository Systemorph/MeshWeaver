{
  "name": "meshweaver-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "MeshWeaver on React Native / Expo \u2014 the native leaf pack over @meshweaver/react/core. The MAUI peer.",
  "main": "expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "ios": "expo run:ios",
    "android": "expo run:android",
    "web": "expo start --web",
    "web:export": "expo export --platform web && node scripts/pwa-inject.mjs dist/index.html",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@expo/metro-runtime": "~57.0.12",
    "expo": "~57.0.15",
    "expo-asset": "~57.0.13",
    "expo-audio": "~57.0.4",
    "expo-auth-session": "~57.0.8",
    "expo-constants": "~57.0.13",
    "expo-crypto": "~57.0.1",
    "expo-font": "~57.0.1",
    "expo-status-bar": "~57.0.1",
    "expo-video": "~57.0.2",
    "expo-web-browser": "~57.0.2",
    "marked": "^18.0.5",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-native": "0.86.2",
    "react-native-fetch-api": "^3.0.0",
    "react-native-render-html": "^6.3.4",
    "react-native-svg": "15.15.4",
    "react-native-web": "~0.21.2",
    "text-encoding": "^0.7.0",
    "web-streams-polyfill": "^4.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-test-renderer": "^19.1.0",
    "react-test-renderer": "19.2.8",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  },
  "_expo_sdk57_note": [
    "Expo SDK 57 + react-native 0.86.2 + react 19.2.8 \u2014 the combination `expo@57`'s own",
    "bundledNativeModules.json names, so every version here is upstream's pairing rather than a",
    "guess. This finishes the migration issue #1584 opened: the npm-major sweep (688ce589b) had",
    "moved expo 52->57 and react 18->19 while PINNING react-native at 0.76.5, which cannot resolve",
    "(RN 0.76 hard-peers react ^18.2.0), and a1ed1eb34 held the whole package back at SDK 52 until",
    "the port could be done deliberately.",
    "",
    "expo-av is GONE from SDK 57 (it is not in bundledNativeModules.json at all) and is replaced by",
    "TWO packages with different APIs \u2014 expo-audio for capture (src/speech/expoRecorder.ts) and",
    "expo-video for the Video leaf (src/rnContainers.tsx). See those files for the per-call-site",
    "notes; the wire format the speech pipeline depends on (16 kHz mono, WAV on iOS / AAC .m4a on",
    "Android) is preserved exactly, because /api/speech/transcribe and the Whisper container are",
    "what consume it.",
    "",
    "The two constraints the SDK 52 holdback recorded are both RETIRED here, each re-measured on",
    "SDK 57 rather than assumed:",
    "  - typescript 7. The blocker was @expo/cli SDK 52 reading tsconfig through the CLASSIC TS",
    "    compiler API that TS 7 deleted ('Cannot read properties of undefined (reading",
    "    getCurrentDirectory)'). SDK 57's CLI does not: `expo export --platform web` runs clean on",
    "    a cold Metro cache under typescript 7.0.2. tsconfig needed two changes for TS 7 itself \u2014",
    "    `baseUrl` is removed (TS5102) and @types is no longer auto-included (hence `types:[node]`,",
    "    the same fix clients/react carries).",
    "  - react 19 / @types/react 19. The blocker was RN 0.76.5's hard `react ^18.2.0` peer; RN",
    "    0.86.2 peers `react ^19.2.3`, which is what the rest of the tree already had.",
    "",
    "Still deliberate, and still enforced by .github/dependabot.yml: react-native, react-native-web",
    "and the expo/@expo majors move as ONE coordinated SDK jump, never as a sweep."
  ],
  "_overrides_note": [
    "postcss: @expo/metro-config pins a version below the 8.5.x patch line. Same major, no API",
    "change, and reachable from what we build (the web export bundles CSS through metro-config)."
  ],
  "overrides": {
    "postcss": "^8.5.23"
  }
}
