// The vendored github-markdown CSS now lives in the SHARED renderer package (one copy for web,
// portal-next, Electron and this app) — re-exported here so existing imports keep working.
export { githubMarkdownCss } from "@meshweaver/react/core";
