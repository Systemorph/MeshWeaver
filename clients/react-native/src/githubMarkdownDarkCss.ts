// The vendored github-markdown dark CSS now lives in the SHARED renderer package (one copy for
// web, portal-next, Electron and this app) — re-exported here so existing imports keep working.
export { githubMarkdownDarkCss } from "@meshweaver/react/core";
