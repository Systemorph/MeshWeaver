{
  "name": "@meshweaver/react",
  "version": "0.1.0",
  "description": "React + Fluent UI renderer for MeshWeaver layout areas — the web/Electron/React-Native-capable counterpart of the MAUI MauiViewPack.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./core": {
      "types": "./dist/core.d.ts",
      "import": "./dist/core.js"
    },
    "./wire": {
      "types": "./dist/live/wire.d.ts",
      "import": "./dist/live/wire.js"
    },
    "./styles": {
      "types": "./dist/render/meshStyles.d.ts",
      "import": "./dist/render/meshStyles.js"
    }
  },
  "files": [
    "dist",
    "docs",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "dev": "vite",
    "demo:build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "electron": "electron electron/main.cjs",
    "prepack": "npm run build"
  },
  "peerDependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "dependencies": {
    "@fluentui/react-components": "^9.54.0",
    "@fluentui/react-icons": "^2.0.260",
    "@monaco-editor/react": "^4.7.0",
    "chart.js": "^4.4.1",
    "react-markdown": "^9.0.1",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.10.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^6.0.5",
    "jsdom": "^29.1.1",
    "mermaid": "11.16.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "typescript": "^5.6.0",
    "vite": "^7.3.6",
    "vitest": "^4.1.9"
  },
  "_fluentui_note": [
    "package-lock.json holds @fluentui/react-components at 9.74.5 ON PURPOSE, and CI installs with",
    "`npm ci`. 9.74.6 (2026-08-11) rewrote its exports map and DROPPED the \"node\" condition that",
    "used to steer Node to the CommonJS build. Node/vitest now resolve the ESM build, which pulls",
    "@fluentui/react-icons' lib/index.js — whose specifiers are EXTENSIONLESS (`export * from",
    "'./icons/chunk-0'`), invalid under Node ESM. Result: `Cannot find module",
    "'@fluentui/react-icons/lib/icons/chunk-0'` in 23 of 34 test suites. The caret stays as-is so",
    "consumers of the published package resolve their own tree; the LOCK is what pins this repo.",
    "Before regenerating the lock, verify upstream ships extensions in react-icons' lib/ ESM build."
  ],
  "_overrides_note": [
    "dompurify: monaco-editor (via @monaco-editor/react) pins 3.2.7 exactly, and even its latest",
    "0.56.0 pins 3.4.8 — both below the patched 3.4.12. Forced here so this repo's installs are",
    "clean; consumers of the published package resolve their own tree (npm ignores a dependency's",
    "overrides), which is why @monaco-editor/react stays a plain dependency."
  ],
  "overrides": {
    "dompurify": "^3.4.12"
  }
}
