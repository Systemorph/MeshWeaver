{
  "$type": "MeshNode",
  "id": "Data",
  "namespace": "Testing",
  "path": "Testing/Data",
  "mainNode": "Testing/Data",
  "name": "MeshWeaver.Data.Test (in-mesh)",
  "description": "MeshWeaver.Data.Test migrated in-mesh (.github/scripts/generate-in-mesh-suites.py, 2026-09-13): 28 of 79 files, 209 [MeshFact]/[MeshTheory] cases, run by MeshWeaver.Testing.InMesh.MeshTestRunner (#4184) in the samples gate's mesh; InMeshTestBase and XunitShims laid in from .github/scripts/in-mesh/. Refused, still on xunit: 51 — AReAsksNotFoundIsAVerdictTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; AccessRestrictionTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; AutocompleteSettlesOnConvergenceTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; BuildMustNotConstructHubsTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ChangeFeedResubscribeCoalesceTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DataChangeLogTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DataContextDisposeDuringInitTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DataContextInitWatchdogTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DataContextShutdownGateAnswersDeferredTest.cs: configures the host hub (GetHost(config)) — the pre-boot substitution facility; DataSynchronizationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DataTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DataValidationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DeadStreamSafetyTest.cs: configures the host hub (GetHost(config)) — the pre-boot substitution facility; EarlyFirstHeartbeatTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; EvictedUnleasedStreamRetentionTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; FaultedProviderIsRebuiltTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; GetStreamUnknownTypeContractTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; HeartbeatFireAndForgetTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; NonExistentOwnerNoHeartbeatStormTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; OneShotReadTotalityTest.cs: uses Microsoft.Reactive.Testing's TestScheduler — not a platform assembly; PatchAckTotalityTest.cs: uses Microsoft.Reactive.Testing's TestScheduler — not a platform assembly; ReadDuringDisposalWindowTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ReadPathStreamMintingTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; RecycleReAskRidesOutTheDyingActivationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; RemoteStreamDisposeCancelsSubscribeTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; RepeatedNoOpReceiptTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ResubscribeKeepsNegotiatedCapabilitiesTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ResubscribeReassertFrameTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SameValuePatchKeepsTheChainTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SchemaTests.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SerializationAndSchemaTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamFrameLossResyncTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamLivenessIsConsumerReachableTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamReleasesItsHubTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamResyncAnswerInFlightTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamResyncConvergenceTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamResyncGivesUpTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamRouteSelfParentTerminationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamUpdateBeforeSubscribeTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamUpdateIdentityTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StreamVersionMonotonicityTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StringDeltaTransportTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SyncHubGoneStreamDroppedTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SyncHubRegistrationRaceTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SynchronizationStreamTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; TeardownPendingSubscribeGracefulTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; TransientInitializationFaultRetiresTheActivationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; UnservedVerdictEvictionTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; VirtualDataSourceProviderFaultTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; VirtualDataSourceTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; WorkspaceDataTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake",
  "nodeType": "NodeType",
  "category": "Types",
  "state": "Active",
  "content": {
    "$type": "NodeTypeDefinition",
    "description": "An in-mesh test suite.",
    "configuration": "config => config.WithContentType<MeshWeaver.Markdown.MarkdownContent>().AddMarkdownViews().AddLayout(layout => layout.WithView(\"Tests\", (host, ctx) => MeshWeaver.Testing.InMesh.MeshTestRunner.Area(host, \"Testing/Data\", typeof(AReAsksNotFoundIsAVerdictTest).Assembly)))",
    "includeGlobalTypes": true,
    "sources": [
      "namespace:Source scope:subtree"
    ]
  }
}
