{
  "$type": "MeshNode",
  "id": "HostingOrleans",
  "namespace": "Testing",
  "path": "Testing/HostingOrleans",
  "mainNode": "Testing/HostingOrleans",
  "name": "MeshWeaver.Hosting.Orleans.Test (in-mesh)",
  "description": "MeshWeaver.Hosting.Orleans.Test migrated in-mesh (.github/scripts/generate-in-mesh-suites.py, 2026-09-13): 18 of 73 files, 47 [MeshFact]/[MeshTheory] cases, run by MeshWeaver.Testing.InMesh.MeshTestRunner (#4184) in the samples gate's mesh; InMeshTestBase and XunitShims laid in from .github/scripts/in-mesh/. Refused, still on xunit: 55 — ActivationLeavesNoOwnCacheSubscriberTest.cs: awaits a mesh read/write directly (HubReachableAsyncGuard.NoNewAwaitOfAMeshRead) — compose reactively and subscribe, or wait through ObserveCompletion; ClusterDisposalRetentionTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; DeadSubscriberEvictionTest.cs: acts as an arbitrary identity — declined by design; DeadTargetRefusalWindowTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; GrainAcknowledgementCarriesNoPayloadTest.cs: uses an xunit fixture — needs a hand port; GrainActivationFailureRegistryTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; GrainActivationSiteRatchetGuard.cs: needs the Orleans silo host — the gate's mesh is the monolith; MessageHubGrainActivationSourceTest.cs: uses Microsoft.Reactive.Testing's TestScheduler — not a platform assembly; OrleansAssemblyStoreTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansBootstrapSeamTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansBrokenNodeTypeAccessTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; OrleansClusterCollection.cs: uses an xunit fixture — needs a hand port; OrleansCompileActivityAccessTest.cs: awaits a mesh read/write directly (HubReachableAsyncGuard.NoNewAwaitOfAMeshRead) — compose reactively and subscribe, or wait through ObserveCompletion; OrleansContentImportSyncTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; OrleansCrossSiloReplyTest.cs: uses an xunit fixture — needs a hand port; OrleansCrossSiloStreamProbeTest.cs: uses an xunit fixture — needs a hand port; OrleansDirectoryInstabilityClassificationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansDocumentationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansDynamicCompilationTest.cs: needs FileSystem persistence — stays on xunit; OrleansEventSubscriptionTimerTest.cs: awaits a mesh read/write directly (HubReachableAsyncGuard.NoNewAwaitOfAMeshRead) — compose reactively and subscribe, or wait through ObserveCompletion; OrleansGetDataRequestPropagationTest.cs: awaits a mesh read/write directly (HubReachableAsyncGuard.NoNewAwaitOfAMeshRead) — compose reactively and subscribe, or wait through ObserveCompletion; OrleansGraphDataTest.cs: needs FileSystem persistence — stays on xunit; OrleansInteractiveMarkdownTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansPubSubStoreConfigurationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansRoutingShutdownClassificationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansStaticRepoImportTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; OrleansStreamingReadinessGateTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansSubscribeRequestNotFoundSurfaceTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansThreeNodePropagationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OrleansUnresolvableNodeNoWedgeTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OversizedGrainDeliveryRefusedTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OversizedRouterDispatchRefusedTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OversizedStreamMessageRefusedTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; PartitionRootActivationTest.cs: bridges an observable to a Task with .ToTask( — forbidden in every gated root (ObservableToTaskBridgeGuard); compose reactively first; PodHubClaimLifetimeTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; PodHubClaimMembershipWiringTest.cs: uses an xunit fixture — needs a hand port; PodHubClaimReassertionTest.cs: bridges an observable to a Task with .ToTask( — forbidden in every gated root (ObservableToTaskBridgeGuard); compose reactively first; PodHubNotHereRefusalTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; PodHubTransportTest.cs: uses an xunit fixture — needs a hand port; RoutedFailureClassificationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingAfterScopeTeardownTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingBackpressureShapeTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingFailureTakesTheLocalRouteTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingGrainDeliveryRetryTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingGrainFailureClassificationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingGrainPodHubDeliveryRetryTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingGrainStreamPostBoundTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingGrainTurnIsolationTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingQuiescenceClusterTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RoutingQuiescenceTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; SiloShutdownActivationGateTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; StreamAttachTransientRetryTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; StreamPostTimeoutAttributionTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; StreamRoutedSilenceGateTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; TestClusterSizingGuard.cs: needs the Orleans silo host — the gate's mesh is the monolith",
  "nodeType": "NodeType",
  "category": "Types",
  "state": "Active",
  "content": {
    "$type": "NodeTypeDefinition",
    "description": "An in-mesh test suite.",
    "configuration": "config => config.WithContentType<MeshWeaver.Markdown.MarkdownContent>().AddMarkdownViews().AddLayout(layout => layout.WithView(\"Tests\", (host, ctx) => MeshWeaver.Testing.InMesh.MeshTestRunner.Area(host, \"Testing/HostingOrleans\", typeof(ActivationLeavesNoOwnCacheSubscriberTest).Assembly)))",
    "includeGlobalTypes": true,
    "sources": [
      "namespace:Source scope:subtree"
    ]
  }
}
