{
  "$type": "MeshNode",
  "id": "Layout",
  "namespace": "Testing",
  "path": "Testing/Layout",
  "mainNode": "Testing/Layout",
  "name": "MeshWeaver.Layout.Test (in-mesh)",
  "description": "MeshWeaver.Layout.Test migrated in-mesh (.github/scripts/generate-in-mesh-suites.py, 2026-09-13): 18 of 60 files, 178 [MeshFact]/[MeshTheory] cases, run by MeshWeaver.Testing.InMesh.MeshTestRunner (#4184) in the samples gate's mesh; InMeshTestBase and XunitShims laid in from .github/scripts/in-mesh/. Refused, still on xunit: 42 — AnalysisControlsTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; AreaChildDiffTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; AreaStreamRetryTest.cs: uses Microsoft.Reactive.Testing's TestScheduler — not a platform assembly; CatalogActionIdentityTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ControlsSerializationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DataChangeStreamUpdateTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DebounceTest.cs: uses Microsoft.Reactive.Testing's TestScheduler — not a platform assembly; DomainLayoutServiceTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DroppedUserActionIsRefusedTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; EditorTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; GetDataStreamUnsetIdTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; HubDisposalRenderTransientTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; InlineEditingTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; LayoutAreaViewerTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; LayoutClientExtensionsTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; LayoutSerializationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; LayoutTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; MapToToggleableControlTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; NativeRendererDataPathTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; NavMenuExtensionsTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; NavigateToSidePanelTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; OptionPolymorphismTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; PandasDataGridWireTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; PandasExplorerAreaTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; PromptCellTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; PropertyCellSizingTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; QueryInRenderDeadlockTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ReadBudgetTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; RenderFailureDiagnosticsTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ScopeTeardownRenderTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SkinSerializationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SlideShowControlTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StaleBaseSubscriberWriteTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StartWithSynchronizationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StatusGatedRenderingTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; StorageUnavailableRenderTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SubscribeDuringRecycleTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; TornDownStreamCallSitesTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; UserActionAcceptedTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; UserActionOutlivesStreamReleaseTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; VideoControlSerializationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ViewDispatchFallbackWarningTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake",
  "nodeType": "NodeType",
  "category": "Types",
  "state": "Active",
  "content": {
    "$type": "NodeTypeDefinition",
    "description": "An in-mesh test suite.",
    "configuration": "config => config.WithContentType<MeshWeaver.Markdown.MarkdownContent>().AddMarkdownViews().AddLayout(layout => layout.WithView(\"Tests\", (host, ctx) => MeshWeaver.Testing.InMesh.MeshTestRunner.Area(host, \"Testing/Layout\", typeof(AreaErrorClassifierTest).Assembly)))",
    "includeGlobalTypes": true,
    "sources": [
      "namespace:Source scope:subtree"
    ]
  }
}
