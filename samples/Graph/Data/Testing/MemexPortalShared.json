{
  "$type": "MeshNode",
  "id": "MemexPortalShared",
  "namespace": "Testing",
  "path": "Testing/MemexPortalShared",
  "mainNode": "Testing/MemexPortalShared",
  "name": "Memex.Portal.Shared.Test (in-mesh)",
  "description": "Memex.Portal.Shared.Test migrated in-mesh (.github/scripts/generate-in-mesh-suites.py, 2026-09-13): 86 of 165 files, 645 [MeshFact]/[MeshTheory] cases, run by MeshWeaver.Testing.InMesh.MeshTestRunner (#4184) in the samples gate's mesh; InMeshTestBase and XunitShims laid in from .github/scripts/in-mesh/. Refused, still on xunit: 79 — AdminVerdictFailsClosedTest.cs: bridges an observable to a Task with .ToTask( — forbidden in every gated root (ObservableToTaskBridgeGuard); compose reactively first; AnInstallHoldsItsRootTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; AnInstanceReportsWhatItRunsTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; BuildPrincipalAuthenticationTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; CatalogCategoryFirstTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; CatalogOrphanActionIdentityTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; ComboGateRollTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; ComboVerdictRecordingTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; ConfigurationHandOverTest.cs: needs Postgres — stays on xunit; DeploymentPathsSupplyStorageTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; DrainEndpointTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; EaCredentialReadTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; EmailCopiedRecipientsTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; EmailSendAsCapabilityTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; ExecuteScriptPreflightTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; FakeRegistry.cs: needs Postgres — stays on xunit; HealthCensusTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; InstallCompletenessTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; InstallNeverInheritsRepoCompileVerdictTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; InstallRecordFollowsItsPartitionTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; InstallTimePrebuiltAdoptionTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; InstanceKeyAdoptionTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; InstanceKeyLiveResolutionTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; InstanceKeyRotationTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; InstanceManifestProjectionTest.cs: needs Postgres — stays on xunit; InstanceManifestTest.cs: needs Postgres — stays on xunit; InstanceOpenRegistrationTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; LocalPartCollisionTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; LocalSourceContentVersionTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; McpBackConnectionEvictionTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; McpRoutingTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; MeshApiCookieAuthTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; ModuleFloorAdvisoryFunnelTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; ModulePlatformFloorScriptParityTest.cs: spawns a process — stays on xunit; ModulePublishShelfTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; MountedCheckoutReassertsOnEveryBootTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; NonAdminUpdateStatusTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; OAuthCodeStoreTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; OciRegistryClientTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; OciTagListerTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PackageInstallRebuildsDependentsTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PackageUpdateReminderIdempotenceTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PluginBundleAnchorTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PluginBundleArtifactFetchTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PluginBundleAuthTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; PluginBundleEntitlementTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PluginBundleIndexArtifactTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PluginBundlePlanTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PluginBundlePublishAssetsTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; PluginBundleSealedLaneTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; PrebuiltPublicationTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; ProbeSeparationTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; PublicSiteTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; PublishedModuleVersionShelfTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; RegistryReconcileDeferralTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; RegistryUpdateReconcilerTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; ReleaseGateAuthTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; RollMidSessionDrainTest.cs: spawns a process — stays on xunit; RollSelectionInventoryTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SealedModuleCompositionTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; SelfUpdateChecksOnlyAtDecisionPointsTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SelfUpdatePendingRestartTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SelfUpdateStrandRecoveryTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SessionDenialIsAnAnswerTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SetupCompositionTest.cs: needs Postgres — stays on xunit; SetupIdentityPhaseTest.cs: needs Postgres — stays on xunit; SetupSurfaceTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; SignInReadsAreAnchoredTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SitemapDescentTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SitemapRootWindowTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; SlashBearingNodeIdKeyingTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; StorageSectionWithoutTypeIsNotConfiguredTest.cs: needs Postgres — stays on xunit; UnanchoredQueryCensusTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; UndeliveredModuleIsRecordedTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; UnreadablePolicyRecordIsNotClobberedTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; UpdatePolicyMcpPatchTest.cs: overrides ConfigureMesh — needs the per-suite configuration facility; VersionEndpointTest.cs: builds an ASP.NET host — the host assembly is not on the NodeType reference set; ViewerZoneTimestampTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; WhatsNewSatelliteEntryTest.cs: awaits a mesh read/write directly (HubReachableAsyncGuard.NoNewAwaitOfAMeshRead) — compose reactively and subscribe, or wait through ObserveCompletion",
  "nodeType": "NodeType",
  "category": "Types",
  "state": "Active",
  "content": {
    "$type": "NodeTypeDefinition",
    "description": "An in-mesh test suite.",
    "configuration": "config => config.WithContentType<MeshWeaver.Markdown.MarkdownContent>().AddMarkdownViews().AddLayout(layout => layout.WithView(\"Tests\", (host, ctx) => MeshWeaver.Testing.InMesh.MeshTestRunner.Area(host, \"Testing/MemexPortalShared\", typeof(AboutSettingsTabTest).Assembly)))",
    "includeGlobalTypes": true,
    "sources": [
      "namespace:Source scope:subtree"
    ]
  }
}
