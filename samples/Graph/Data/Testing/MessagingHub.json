{
  "$type": "MeshNode",
  "id": "MessagingHub",
  "namespace": "Testing",
  "path": "Testing/MessagingHub",
  "mainNode": "Testing/MessagingHub",
  "name": "MeshWeaver.Messaging.Hub.Test (in-mesh)",
  "description": "MeshWeaver.Messaging.Hub.Test migrated in-mesh (.github/scripts/generate-in-mesh-suites.py, 2026-09-13): 38 of 80 files, 150 [MeshFact]/[MeshTheory] cases, run by MeshWeaver.Testing.InMesh.MeshTestRunner (#4184) in the samples gate's mesh; InMeshTestBase and XunitShims laid in from .github/scripts/in-mesh/. Refused, still on xunit: 42 — AccessContextNeverNullTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; AccessContextSurvivesSubscribeTest.cs: acts as an arbitrary identity — declined by design; DeferralTrackerClaimTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DeferredBacklogBoundedTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DeferredDeliveryNackedOnDisposeTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DeferredTurnsResumeAheadOfLaterArrivalsTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DeletedAddressNackClassificationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DisplayTimeTest.cs: acts as an arbitrary identity — declined by design; DisposalWaitBridgeTest.cs: bridges an observable to a Task with .ToTask( — forbidden in every gated root (ObservableToTaskBridgeGuard); compose reactively first; FateStageTokensAreAContractTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; GateOpenMustNotLetARunningTurnOvertakeTheBacklogTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; HeartBeatEventGateBypassTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; HostedHubTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; HubDisposalFailureClassificationTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; HubDisposesItsServiceProviderTest.cs: needs Postgres — stays on xunit; ImpersonationScopeThreadAffinityTest.cs: opens an impersonation scope with Observable.Using — the in-mesh shape is AsSystem (check-impersonation.py); needs a hand port; InfrastructureFaultTest.cs: needs Postgres — stays on xunit; InitializationErrorSurfacedTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; InitializationGateBypassTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; InlineResumptionMechanismTest.cs: bridges an observable to a Task with .ToTask( — forbidden in every gated root (ObservableToTaskBridgeGuard); compose reactively first; LocalizationTest.cs: acts as an arbitrary identity — declined by design; MessageHubTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; OversizedFailureReportSurvivesTest.cs: needs the Orleans silo host — the gate's mesh is the monolith; OwnedConnectionTest.cs: uses Microsoft.Reactive.Testing's TestScheduler — not a platform assembly; PersistenceRunsAsSystemTest.cs: acts as an arbitrary identity — declined by design; PostPipelineAccessContextTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; QuiescingTimeoutNamesHandlerSideTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ReplyTrailFollowsTheRequestTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; RequestFateTrailOutlivesTheCallbackTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; ResponseFromContinuationAccessContextTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; RouterTrafficDetectorTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; RoutingFailureReportedTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; RuleChainConcurrentDisposeTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; SystemScopeDoesNotEscapeTest.cs: opens an impersonation scope with Observable.Using — the in-mesh shape is AsSystem (check-impersonation.py); needs a hand port; TimeoutMessageNamesTheCallersOwnStateTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; TypeRegistryTest.cs: overrides the fixture's Configure(Host|Client|Mesh) — the pre-boot service substitution facility, not something the live mesh can fake; DisposeDuringInitializationTest.cs: uses InitializationErrorSurfacedTest declared in a refused file; HubDisposalJoinTest.cs: uses DisposalWaitBridgeTest declared in a refused file; ObjectAsExtensionsTest.cs: uses Probe declared in a refused file; PolymorphicResolverUnloadableAssemblyTest.cs: uses Probe declared in a refused file; RouterTrafficRuleTest.cs: uses RouterTrafficDetectorTest declared in a refused file; ScopeDisposedDuringInitializationTest.cs: uses InitializationErrorSurfacedTest declared in a refused file",
  "nodeType": "NodeType",
  "category": "Types",
  "state": "Active",
  "content": {
    "$type": "NodeTypeDefinition",
    "description": "An in-mesh test suite.",
    "configuration": "config => config.WithContentType<MeshWeaver.Markdown.MarkdownContent>().AddMarkdownViews().AddLayout(layout => layout.WithView(\"Tests\", (host, ctx) => MeshWeaver.Testing.InMesh.MeshTestRunner.Area(host, \"Testing/MessagingHub\", typeof(AccessContextNeverNullTest).Assembly)))",
    "includeGlobalTypes": true,
    "sources": [
      "namespace:Source scope:subtree"
    ]
  }
}
