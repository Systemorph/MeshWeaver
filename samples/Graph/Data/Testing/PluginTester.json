{
  "$type": "MeshNode",
  "id": "PluginTester",
  "namespace": "Testing",
  "path": "Testing/PluginTester",
  "mainNode": "Testing/PluginTester",
  "name": "MeshWeaver.PluginTester.Test (in-mesh)",
  "description": "MeshWeaver.PluginTester.Test migrated in-mesh (.github/scripts/generate-in-mesh-suites.py, 2026-09-13): 28 of 41 files, 247 [MeshFact]/[MeshTheory] cases, run by MeshWeaver.Testing.InMesh.MeshTestRunner (#4184) in the samples gate's mesh; InMeshTestBase and XunitShims laid in from .github/scripts/in-mesh/. Refused, still on xunit: 13 — AreaProbeTest.cs: bridges an observable to a Task with .ToTask( — forbidden in every gated root (ObservableToTaskBridgeGuard); compose reactively first; BakeAgainstPlatformHostTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; BakeCompilesAgainstModulesTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; BakeEquivalenceTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; BakeGateSplitTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; BakeOutputTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; BundleCarriesTheUpstreamTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; EmbeddedResourceTest.cs: spawns a process — stays on xunit; ExternalModuleGateTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; GateInstallsUpstreamPackagesTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; GateServesPackageContentTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; MeshTestSuiteTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit; PluginGateRunnerTest.cs: reads the repository tree from the test bin (a source-scanning guard) — no tree in a mesh; stays on xunit",
  "nodeType": "NodeType",
  "category": "Types",
  "state": "Active",
  "content": {
    "$type": "NodeTypeDefinition",
    "description": "An in-mesh test suite.",
    "configuration": "config => config.WithContentType<MeshWeaver.Markdown.MarkdownContent>().AddMarkdownViews().AddLayout(layout => layout.WithView(\"Tests\", (host, ctx) => MeshWeaver.Testing.InMesh.MeshTestRunner.Area(host, \"Testing/PluginTester\", typeof(AreaProbeTest).Assembly)))",
    "includeGlobalTypes": true,
    "sources": [
      "namespace:Source scope:subtree"
    ]
  }
}
