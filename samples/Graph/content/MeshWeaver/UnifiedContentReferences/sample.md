# Sample Embedded Content

This is a **sample markdown file** that demonstrates content embedding using the `@@` syntax with the `content:` prefix.

## Features

- Supports **bold** and *italic* text
- Code blocks like `inline code`
- Lists and headers

This content was embedded from a separate file!
