@page "/northwind"
@using OpenSmc.Application;
@using OpenSmc.Layout;
@using OpenSmc.Northwind.ViewModel

<PageTitle>Northwind</PageTitle>

<LayoutAreaView Address="@address" Reference="@reference"/>

@code{
    private readonly object address = new ApplicationAddress("Northwind", "dev");
    private readonly LayoutAreaReference reference = new(nameof(NorthwindViewModels.Dashboard)); 
}