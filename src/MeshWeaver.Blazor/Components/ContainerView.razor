@inherits BlazorView<IContainerControl, ContainerView>

@foreach (var area in ViewModel.Areas)
{
    <DispatchView ViewModel="@area" Stream="@Stream" Area="@area.GetArea()" DataContext="@DataContext" />
}
