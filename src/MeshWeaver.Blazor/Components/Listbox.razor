@inherits ListBase<ListboxControl, Listbox>

<FluentListbox 
    Label="@Label"
    Items="@Options"
    TOption="Option"
    OptionText="@(x => x.Text)"
    OptionValue="@(x => x.ItemString)"
    Class="@Class"
    @bind-SelectedOption="Value"
/>