@page "/article/{**Path}"
@using MeshWeaver.Blazor.Infrastructure
@inject PortalApplication PortalApplication

<LayoutArea Address="@PortalApplication.Hub.Address" Area="Articles" Id="@Path"></LayoutArea>

@code
{
    [Parameter] public string? Path { get; set; }
}