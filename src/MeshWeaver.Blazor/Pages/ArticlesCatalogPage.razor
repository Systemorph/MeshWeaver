@page "/articles/{Collection}/{**Options}"
@using MeshWeaver.Mesh

<LayoutArea Address="@Address" Area="Catalog" Id="@Options"></LayoutArea>
@code
{
    private ArticleAddress Address => new(Collection);
    [Parameter] public string Collection { get; set; }
    [Parameter] public string Options { get; set; }
}