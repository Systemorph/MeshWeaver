### Sample embedded markdown

This file lives in the **`content`** collection of the *Unified Path* node and is embedded inline
via `@@content/sample.md`. Content-collection files like this ship with the docs and are synced
into the node's content collection on import — see
[Static-Repo Import](@/Doc/Architecture/StaticRepoImport).
