<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>The OgCard link-preview layout area for MeshWeaver — one card or a responsive grid for external pages (server-side Open Graph fetch) or mesh nodes, embedded from markdown. Ships as a module: list the DLL under Modules:Assemblies to activate.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\MeshWeaver.Graph\MeshWeaver.Graph.csproj" />
  </ItemGroup>

</Project>
