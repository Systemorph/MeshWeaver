@using OpenSmc.Data.Serialization
@typeparam TViewModel

@code {
    [Parameter] public TViewModel ViewModel { get; set; }

    [Parameter] public IChangeStream<EntityStore, LayoutAreaReference> Stream { get; set; }

}