@inherits ListBase<ListboxControl>

<FluentListbox 
    Label="@Label"
    Items="@Options"
    TOption="Option"
    OptionText="@(x => x.Text)"
    OptionValue="@(x => x.ItemString)"
    @bind-SelectedOption="Selected"
/>