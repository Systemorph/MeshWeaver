@inherits BlazorView<NavMenuControl>

@* todo: un-hardcode Id *@
<FluentNavMenu Id="main-menu" Collapsible="@ViewModel.Collapsible" Width="@ViewModel.Width" @bind-Expanded="@expanded">
    @foreach (var item in ViewModel.Items)
    {
        // TODO V10: should probably create subareas? (09.06.2024, Roland Bürgi)
        <DispatchView ViewModel="@item" Stream="@Stream" Area ="@Area" />
    }
</FluentNavMenu>

@code {
    private bool expanded = true;
}