@using Orientation = Microsoft.FluentUI.AspNetCore.Components.Orientation
@inherits SkinnedView<LayoutStackControl, SplitterSkin>
@{
    #pragma warning disable FluentMultiSplitter
}

<FluentMultiSplitter
    Class="@Class"
    Orientation="@Orientation"
    BarSize="@Skin.BarSize"
    Width="@ViewModel.Width"
    Height="@ViewModel.Height"
    Style="@Style">
    
    @foreach(var area in ViewModel.Areas) 
    {
        <NamedAreaView Stream="@Stream" Area="@area"/>
    }
</FluentMultiSplitter>

@code
{
    private Orientation Orientation => @ViewModel.Orientation?.ToFluentOrientation() ?? Orientation.Horizontal;
}
