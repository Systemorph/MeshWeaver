<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <ProjectGuid>{d3e4f5a6-2b7c-4d8e-9f0a-1b2c3d4e5f6a}</ProjectGuid>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\MeshWeaver.Documentation\MeshWeaver.Documentation.csproj" />
    <ProjectReference Include="..\MeshWeaver.Hosting.Monolith.TestBase\MeshWeaver.Hosting.Monolith.TestBase.csproj" />
    <!-- PlatformBakeLaneGuard pins the bake lane's published layout against the constants that
         READ it (the _complete sentinel, the _releases marker directory), so the guard has to see
         the reader's own constants rather than restate them. -->
    <ProjectReference Include="..\..\src\MeshWeaver.PluginCatalog\MeshWeaver.PluginCatalog.csproj" />
    <!-- ReleaseDeliveryChainGuard pins the release-broadcast chain's CONFIGURATION KEYS against the
         constants that read them (FrameworkBroadcastOptions.SubscribersEnvKeyPrefix), for the same
         reason as above: a guard that restates a key instead of referencing it goes green through
         the rename it exists to catch. -->
    <ProjectReference Include="..\..\src\MeshWeaver.GitSync\MeshWeaver.GitSync.csproj" />
    <!-- RouterAsNodeOperationOriginRatchetGuard DERIVES its denominator from these two assemblies'
         own lifecycle registrations (MessageHub's constructor; MeshExtensions.WithNodeOperationHandlers)
         and filters it through RouterTrafficRule, the same predicate the runtime detector evaluates.
         Both arrive transitively already; naming them is the point — the guard resolves every derived
         name to a real Type, so a rename must red the guard rather than shrink what it measures, and
         that only holds while these references cannot quietly go away. -->
    <ProjectReference Include="..\..\src\MeshWeaver.Messaging.Hub\MeshWeaver.Messaging.Hub.csproj" />
    <ProjectReference Include="..\..\src\MeshWeaver.Mesh.Contract\MeshWeaver.Mesh.Contract.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="YamlDotNet" />
    <!-- ContentRoute503DiscriminatorGuard drives ReadBudget's timer in VIRTUAL time: the budget it
         pins is 10 s, and a guard that spent 10 s of wall clock to read one string would be the
         first thing anyone deletes. -->
    <PackageReference Include="Microsoft.Reactive.Testing" />
  </ItemGroup>

</Project>
