{
  "name": "open-smc",
  "scripts": {
    "clean-node-modules": "npm exec --workspaces -- npx rimraf node_modules && npx rimraf node_modules"
  },
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.2.2"
  }
}
