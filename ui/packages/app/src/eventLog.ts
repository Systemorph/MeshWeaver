export {}

// ApplicationHub.onMessage(null, null, ({$type, ...props}, direction, viewModelId) => {
//     const color = direction === 'Down' ? 'green' : 'red';
//     console.log(direction === 'Down' ? '%c↓' : '%c↑', `color: ${color}`, viewModelId, {$type, ...props});
// });