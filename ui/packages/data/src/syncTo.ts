import { MessageHub } from "@open-smc/message-hub/src/api/MessageHub";
import { Store } from "@reduxjs/toolkit";

export function syncTo(store: Store, hub: MessageHub, workspaceReference: any) {
    const {dispatch} = store;



    return store;
}

